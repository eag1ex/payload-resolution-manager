/**
 * @PRMsend
 */
module.exports = (PRM) => {
    return class PRMaccept extends PRM {
        constructor(debug, settings) {
            super(debug, settings)
        }

        send() {

        }
    }
}
